spring:
  application:
   name: appointments-service

  jpa:
   hibernate:
     ddl-auto: validate
   show-sql: true
   properties:
    hibernate:
     format_sql: true

  flyway:
   enabled: true
   locations: classpath:db/migration
   baseline-on-migrate: true
server:
  port: 8084

springdoc:
  api-docs:
   path: /v3/api-docs
  swagger-ui:
   path: /swagger-ui.html
   operationsSorter: method
   tagsSorter: alpha
  enable-hateoas: false

logging:
  level:
   org.hibernate.SQL: debug
   org.hibernate.type.descriptor.sql.BasicBinder: trace


services:
  patients:
    base-url: http://localhost:8082
  doctors:
    base-url: http://localhost:8081
  billing:
    base-url: http://localhost:8083

#services:
#  patients:
#    base-url: http://patients-service:8082
#  doctors:
#    base-url: http://doctors-service:8081
#  billing:
#    base-url: http://billing-service:8083